class Triangle {
    boolean isJunk;

    Triangle(color) {
        this.isJunk = false;
    }

    /*
    void show() {
        noStroke();
        translate(width*0.2, height*0.5);
        rotate(frameCount / 200.0);
        //fill('blue', 200);
        fill('green');
        polygon(0, 0, 82, 3);
    }
    */
}
